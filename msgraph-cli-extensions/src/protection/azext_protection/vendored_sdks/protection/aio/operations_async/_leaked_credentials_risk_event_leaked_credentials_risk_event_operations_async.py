# coding=utf-8
# --------------------------------------------------------------------------
# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License. See License.txt in the project root for license information.
# Code generated by Microsoft (R) AutoRest Code Generator.
# Changes may cause incorrect behavior and will be lost if the code is regenerated.
# --------------------------------------------------------------------------
import datetime
from typing import Any, AsyncIterable, Callable, Dict, Generic, List, Optional, TypeVar, Union
import warnings

from azure.core.async_paging import AsyncItemPaged, AsyncList
from azure.core.exceptions import HttpResponseError, ResourceExistsError, ResourceNotFoundError, map_error
from azure.core.pipeline import PipelineResponse
from azure.core.pipeline.transport import AsyncHttpResponse, HttpRequest
from azure.mgmt.core.exceptions import ARMErrorFormat

from ... import models

T = TypeVar('T')
ClsType = Optional[Callable[[PipelineResponse[HttpRequest, AsyncHttpResponse], T, Dict[str, Any]], Any]]

class LeakedCredentialsRiskEventLeakedCredentialsRiskEventOperations:
    """LeakedCredentialsRiskEventLeakedCredentialsRiskEventOperations async operations.

    You should not instantiate this class directly. Instead, you should create a Client instance that
    instantiates it for you and attaches it as an attribute.

    :ivar models: Alias to model classes used in this operation group.
    :type models: ~identity_protection.models
    :param client: Client for service requests.
    :param config: Configuration of service client.
    :param serializer: An object model serializer.
    :param deserializer: An object model deserializer.
    """

    models = models

    def __init__(self, client, config, serializer, deserializer) -> None:
        self._client = client
        self._serialize = serializer
        self._deserialize = deserializer
        self._config = config

    def list_leaked_credentials_risk_event(
        self,
        orderby: Optional[List[Union[str, "models.Enum123"]]] = None,
        select: Optional[List[Union[str, "models.Enum124"]]] = None,
        expand: Optional[List[Union[str, "models.Enum125"]]] = None,
        **kwargs
    ) -> AsyncIterable["models.CollectionOfLeakedCredentialsRiskEvent"]:
        """Get entities from leakedCredentialsRiskEvents.

        Get entities from leakedCredentialsRiskEvents.

        :param orderby: Order items by property values.
        :type orderby: list[str or ~identity_protection.models.Enum123]
        :param select: Select properties to be returned.
        :type select: list[str or ~identity_protection.models.Enum124]
        :param expand: Expand related entities.
        :type expand: list[str or ~identity_protection.models.Enum125]
        :keyword callable cls: A custom type or function that will be passed the direct response
        :return: An iterator like instance of either CollectionOfLeakedCredentialsRiskEvent or the result of cls(response)
        :rtype: ~azure.core.async_paging.AsyncItemPaged[~identity_protection.models.CollectionOfLeakedCredentialsRiskEvent]
        :raises: ~azure.core.exceptions.HttpResponseError
        """
        cls = kwargs.pop('cls', None)  # type: ClsType["models.CollectionOfLeakedCredentialsRiskEvent"]
        error_map = {404: ResourceNotFoundError, 409: ResourceExistsError}
        error_map.update(kwargs.pop('error_map', {}))

        def prepare_request(next_link=None):
            # Construct headers
            header_parameters = {}  # type: Dict[str, Any]
            header_parameters['Accept'] = 'application/json'

            if not next_link:
                # Construct URL
                url = self.list_leaked_credentials_risk_event.metadata['url']  # type: ignore
                # Construct parameters
                query_parameters = {}  # type: Dict[str, Any]
                if self._config.top is not None:
                    query_parameters['$top'] = self._serialize.query("self._config.top", self._config.top, 'int', minimum=0)
                if self._config.skip is not None:
                    query_parameters['$skip'] = self._serialize.query("self._config.skip", self._config.skip, 'int', minimum=0)
                if self._config.search is not None:
                    query_parameters['$search'] = self._serialize.query("self._config.search", self._config.search, 'str')
                if self._config.filter is not None:
                    query_parameters['$filter'] = self._serialize.query("self._config.filter", self._config.filter, 'str')
                if self._config.count is not None:
                    query_parameters['$count'] = self._serialize.query("self._config.count", self._config.count, 'bool')
                if orderby is not None:
                    query_parameters['$orderby'] = self._serialize.query("orderby", orderby, '[str]', div=',')
                if select is not None:
                    query_parameters['$select'] = self._serialize.query("select", select, '[str]', div=',')
                if expand is not None:
                    query_parameters['$expand'] = self._serialize.query("expand", expand, '[str]', div=',')

                request = self._client.get(url, query_parameters, header_parameters)
            else:
                url = next_link
                query_parameters = {}  # type: Dict[str, Any]
                request = self._client.get(url, query_parameters, header_parameters)
            return request

        async def extract_data(pipeline_response):
            deserialized = self._deserialize('CollectionOfLeakedCredentialsRiskEvent', pipeline_response)
            list_of_elem = deserialized.value
            if cls:
                list_of_elem = cls(list_of_elem)
            return deserialized.odata_next_link or None, AsyncList(list_of_elem)

        async def get_next(next_link=None):
            request = prepare_request(next_link)

            pipeline_response = await self._client._pipeline.run(request, stream=False, **kwargs)
            response = pipeline_response.http_response

            if response.status_code not in [200]:
                error = self._deserialize(models.OdataError, response)
                map_error(status_code=response.status_code, response=response, error_map=error_map)
                raise HttpResponseError(response=response, model=error, error_format=ARMErrorFormat)

            return pipeline_response

        return AsyncItemPaged(
            get_next, extract_data
        )
    list_leaked_credentials_risk_event.metadata = {'url': '/leakedCredentialsRiskEvents'}  # type: ignore

    async def create_leaked_credentials_risk_event(
        self,
        id: Optional[str] = None,
        user_display_name: Optional[str] = None,
        user_principal_name: Optional[str] = None,
        risk_event_date_time: Optional[datetime.datetime] = None,
        risk_event_type: Optional[str] = None,
        risk_level: Optional[Union[str, "models.MicrosoftGraphRiskLevel"]] = None,
        risk_event_status: Optional[Union[str, "models.MicrosoftGraphRiskEventStatus"]] = None,
        closed_date_time: Optional[datetime.datetime] = None,
        created_date_time: Optional[datetime.datetime] = None,
        user_id: Optional[str] = None,
        impacted_user: Optional["models.MicrosoftGraphUser"] = None,
        **kwargs
    ) -> "models.MicrosoftGraphIdentityRiskEvent":
        """Add new entity to leakedCredentialsRiskEvents.

        Add new entity to leakedCredentialsRiskEvents.

        :param id: Read-only.
        :type id: str
        :param user_display_name:
        :type user_display_name: str
        :param user_principal_name:
        :type user_principal_name: str
        :param risk_event_date_time:
        :type risk_event_date_time: ~datetime.datetime
        :param risk_event_type:
        :type risk_event_type: str
        :param risk_level: riskLevel.
        :type risk_level: str or ~identity_protection.models.MicrosoftGraphRiskLevel
        :param risk_event_status: riskEventStatus.
        :type risk_event_status: str or ~identity_protection.models.MicrosoftGraphRiskEventStatus
        :param closed_date_time:
        :type closed_date_time: ~datetime.datetime
        :param created_date_time:
        :type created_date_time: ~datetime.datetime
        :param user_id:
        :type user_id: str
        :param impacted_user: Represents an Azure Active Directory user object.
        :type impacted_user: ~identity_protection.models.MicrosoftGraphUser
        :keyword callable cls: A custom type or function that will be passed the direct response
        :return: MicrosoftGraphIdentityRiskEvent, or the result of cls(response)
        :rtype: ~identity_protection.models.MicrosoftGraphIdentityRiskEvent
        :raises: ~azure.core.exceptions.HttpResponseError
        """
        cls = kwargs.pop('cls', None)  # type: ClsType["models.MicrosoftGraphIdentityRiskEvent"]
        error_map = {404: ResourceNotFoundError, 409: ResourceExistsError}
        error_map.update(kwargs.pop('error_map', {}))

        _body = models.MicrosoftGraphIdentityRiskEvent(id=id, user_display_name=user_display_name, user_principal_name=user_principal_name, risk_event_date_time=risk_event_date_time, risk_event_type=risk_event_type, risk_level=risk_level, risk_event_status=risk_event_status, closed_date_time=closed_date_time, created_date_time=created_date_time, user_id=user_id, impacted_user=impacted_user)
        content_type = kwargs.pop("content_type", "application/json")

        # Construct URL
        url = self.create_leaked_credentials_risk_event.metadata['url']  # type: ignore

        # Construct parameters
        query_parameters = {}  # type: Dict[str, Any]

        # Construct headers
        header_parameters = {}  # type: Dict[str, Any]
        header_parameters['Content-Type'] = self._serialize.header("content_type", content_type, 'str')
        header_parameters['Accept'] = 'application/json'

        body_content_kwargs = {}  # type: Dict[str, Any]
        body_content = self._serialize.body(_body, 'MicrosoftGraphIdentityRiskEvent')
        body_content_kwargs['content'] = body_content
        request = self._client.post(url, query_parameters, header_parameters, **body_content_kwargs)

        pipeline_response = await self._client._pipeline.run(request, stream=False, **kwargs)
        response = pipeline_response.http_response

        if response.status_code not in [201]:
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = self._deserialize(models.OdataError, response)
            raise HttpResponseError(response=response, model=error, error_format=ARMErrorFormat)

        deserialized = self._deserialize('MicrosoftGraphIdentityRiskEvent', pipeline_response)

        if cls:
            return cls(pipeline_response, deserialized, {})

        return deserialized
    create_leaked_credentials_risk_event.metadata = {'url': '/leakedCredentialsRiskEvents'}  # type: ignore

    async def get_leaked_credentials_risk_event(
        self,
        leaked_credentials_risk_event_id: str,
        select: Optional[List[Union[str, "models.Enum126"]]] = None,
        expand: Optional[List[Union[str, "models.Enum127"]]] = None,
        **kwargs
    ) -> "models.MicrosoftGraphIdentityRiskEvent":
        """Get entity from leakedCredentialsRiskEvents by key.

        Get entity from leakedCredentialsRiskEvents by key.

        :param leaked_credentials_risk_event_id: key: leakedCredentialsRiskEvent-id of
         leakedCredentialsRiskEvent.
        :type leaked_credentials_risk_event_id: str
        :param select: Select properties to be returned.
        :type select: list[str or ~identity_protection.models.Enum126]
        :param expand: Expand related entities.
        :type expand: list[str or ~identity_protection.models.Enum127]
        :keyword callable cls: A custom type or function that will be passed the direct response
        :return: MicrosoftGraphIdentityRiskEvent, or the result of cls(response)
        :rtype: ~identity_protection.models.MicrosoftGraphIdentityRiskEvent
        :raises: ~azure.core.exceptions.HttpResponseError
        """
        cls = kwargs.pop('cls', None)  # type: ClsType["models.MicrosoftGraphIdentityRiskEvent"]
        error_map = {404: ResourceNotFoundError, 409: ResourceExistsError}
        error_map.update(kwargs.pop('error_map', {}))

        # Construct URL
        url = self.get_leaked_credentials_risk_event.metadata['url']  # type: ignore
        path_format_arguments = {
            'leakedCredentialsRiskEvent-id': self._serialize.url("leaked_credentials_risk_event_id", leaked_credentials_risk_event_id, 'str'),
        }
        url = self._client.format_url(url, **path_format_arguments)

        # Construct parameters
        query_parameters = {}  # type: Dict[str, Any]
        if select is not None:
            query_parameters['$select'] = self._serialize.query("select", select, '[str]', div=',')
        if expand is not None:
            query_parameters['$expand'] = self._serialize.query("expand", expand, '[str]', div=',')

        # Construct headers
        header_parameters = {}  # type: Dict[str, Any]
        header_parameters['Accept'] = 'application/json'

        request = self._client.get(url, query_parameters, header_parameters)
        pipeline_response = await self._client._pipeline.run(request, stream=False, **kwargs)
        response = pipeline_response.http_response

        if response.status_code not in [200]:
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = self._deserialize(models.OdataError, response)
            raise HttpResponseError(response=response, model=error, error_format=ARMErrorFormat)

        deserialized = self._deserialize('MicrosoftGraphIdentityRiskEvent', pipeline_response)

        if cls:
            return cls(pipeline_response, deserialized, {})

        return deserialized
    get_leaked_credentials_risk_event.metadata = {'url': '/leakedCredentialsRiskEvents/{leakedCredentialsRiskEvent-id}'}  # type: ignore

    async def update_leaked_credentials_risk_event(
        self,
        leaked_credentials_risk_event_id: str,
        id: Optional[str] = None,
        user_display_name: Optional[str] = None,
        user_principal_name: Optional[str] = None,
        risk_event_date_time: Optional[datetime.datetime] = None,
        risk_event_type: Optional[str] = None,
        risk_level: Optional[Union[str, "models.MicrosoftGraphRiskLevel"]] = None,
        risk_event_status: Optional[Union[str, "models.MicrosoftGraphRiskEventStatus"]] = None,
        closed_date_time: Optional[datetime.datetime] = None,
        created_date_time: Optional[datetime.datetime] = None,
        user_id: Optional[str] = None,
        impacted_user: Optional["models.MicrosoftGraphUser"] = None,
        **kwargs
    ) -> None:
        """Update entity in leakedCredentialsRiskEvents.

        Update entity in leakedCredentialsRiskEvents.

        :param leaked_credentials_risk_event_id: key: leakedCredentialsRiskEvent-id of
         leakedCredentialsRiskEvent.
        :type leaked_credentials_risk_event_id: str
        :param id: Read-only.
        :type id: str
        :param user_display_name:
        :type user_display_name: str
        :param user_principal_name:
        :type user_principal_name: str
        :param risk_event_date_time:
        :type risk_event_date_time: ~datetime.datetime
        :param risk_event_type:
        :type risk_event_type: str
        :param risk_level: riskLevel.
        :type risk_level: str or ~identity_protection.models.MicrosoftGraphRiskLevel
        :param risk_event_status: riskEventStatus.
        :type risk_event_status: str or ~identity_protection.models.MicrosoftGraphRiskEventStatus
        :param closed_date_time:
        :type closed_date_time: ~datetime.datetime
        :param created_date_time:
        :type created_date_time: ~datetime.datetime
        :param user_id:
        :type user_id: str
        :param impacted_user: Represents an Azure Active Directory user object.
        :type impacted_user: ~identity_protection.models.MicrosoftGraphUser
        :keyword callable cls: A custom type or function that will be passed the direct response
        :return: None, or the result of cls(response)
        :rtype: None
        :raises: ~azure.core.exceptions.HttpResponseError
        """
        cls = kwargs.pop('cls', None)  # type: ClsType[None]
        error_map = {404: ResourceNotFoundError, 409: ResourceExistsError}
        error_map.update(kwargs.pop('error_map', {}))

        _body = models.MicrosoftGraphIdentityRiskEvent(id=id, user_display_name=user_display_name, user_principal_name=user_principal_name, risk_event_date_time=risk_event_date_time, risk_event_type=risk_event_type, risk_level=risk_level, risk_event_status=risk_event_status, closed_date_time=closed_date_time, created_date_time=created_date_time, user_id=user_id, impacted_user=impacted_user)
        content_type = kwargs.pop("content_type", "application/json")

        # Construct URL
        url = self.update_leaked_credentials_risk_event.metadata['url']  # type: ignore
        path_format_arguments = {
            'leakedCredentialsRiskEvent-id': self._serialize.url("leaked_credentials_risk_event_id", leaked_credentials_risk_event_id, 'str'),
        }
        url = self._client.format_url(url, **path_format_arguments)

        # Construct parameters
        query_parameters = {}  # type: Dict[str, Any]

        # Construct headers
        header_parameters = {}  # type: Dict[str, Any]
        header_parameters['Content-Type'] = self._serialize.header("content_type", content_type, 'str')

        body_content_kwargs = {}  # type: Dict[str, Any]
        body_content = self._serialize.body(_body, 'MicrosoftGraphIdentityRiskEvent')
        body_content_kwargs['content'] = body_content
        request = self._client.patch(url, query_parameters, header_parameters, **body_content_kwargs)

        pipeline_response = await self._client._pipeline.run(request, stream=False, **kwargs)
        response = pipeline_response.http_response

        if response.status_code not in [204]:
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = self._deserialize(models.OdataError, response)
            raise HttpResponseError(response=response, model=error, error_format=ARMErrorFormat)

        if cls:
            return cls(pipeline_response, None, {})

    update_leaked_credentials_risk_event.metadata = {'url': '/leakedCredentialsRiskEvents/{leakedCredentialsRiskEvent-id}'}  # type: ignore

    async def delete_leaked_credentials_risk_event(
        self,
        leaked_credentials_risk_event_id: str,
        if_match: Optional[str] = None,
        **kwargs
    ) -> None:
        """Delete entity from leakedCredentialsRiskEvents.

        Delete entity from leakedCredentialsRiskEvents.

        :param leaked_credentials_risk_event_id: key: leakedCredentialsRiskEvent-id of
         leakedCredentialsRiskEvent.
        :type leaked_credentials_risk_event_id: str
        :param if_match: ETag.
        :type if_match: str
        :keyword callable cls: A custom type or function that will be passed the direct response
        :return: None, or the result of cls(response)
        :rtype: None
        :raises: ~azure.core.exceptions.HttpResponseError
        """
        cls = kwargs.pop('cls', None)  # type: ClsType[None]
        error_map = {404: ResourceNotFoundError, 409: ResourceExistsError}
        error_map.update(kwargs.pop('error_map', {}))

        # Construct URL
        url = self.delete_leaked_credentials_risk_event.metadata['url']  # type: ignore
        path_format_arguments = {
            'leakedCredentialsRiskEvent-id': self._serialize.url("leaked_credentials_risk_event_id", leaked_credentials_risk_event_id, 'str'),
        }
        url = self._client.format_url(url, **path_format_arguments)

        # Construct parameters
        query_parameters = {}  # type: Dict[str, Any]

        # Construct headers
        header_parameters = {}  # type: Dict[str, Any]
        if if_match is not None:
            header_parameters['If-Match'] = self._serialize.header("if_match", if_match, 'str')

        request = self._client.delete(url, query_parameters, header_parameters)
        pipeline_response = await self._client._pipeline.run(request, stream=False, **kwargs)
        response = pipeline_response.http_response

        if response.status_code not in [204]:
            map_error(status_code=response.status_code, response=response, error_map=error_map)
            error = self._deserialize(models.OdataError, response)
            raise HttpResponseError(response=response, model=error, error_format=ARMErrorFormat)

        if cls:
            return cls(pipeline_response, None, {})

    delete_leaked_credentials_risk_event.metadata = {'url': '/leakedCredentialsRiskEvents/{leakedCredentialsRiskEvent-id}'}  # type: ignore
